const esoterics = {
  // Whitespace programming language
  whitespace: ["S", "T"], // Spaces and tabs

  // BrainF
  brainF: [">", "<", ",", "\\[", "\\]", "\\.", "\\+", "\\-"],
};

export default esoterics;
